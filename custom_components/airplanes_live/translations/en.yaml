{
  "config": {
    "step": {
      "user": { "title": "Mode", "data": { "tracking_mode": "Mode" } },
      "zone": { "title": "Zone Settings", "data": { "latitude": "Lat", "longitude": "Lon", "radius": "Radius (meters)" } }
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Configuration",
        "data": {
          "radius": "Radius (meters)",
          "add_track": "Add to track (Callsign or Hex)",
          "remove_track": "Remove from track (Callsign or Hex)",
          "clear_track": "Clear Additional tracked"
        }
      }
    }
  }
}